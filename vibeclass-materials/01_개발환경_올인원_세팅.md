1회차: 개발환경 올인원 세팅

목표: 처음부터 끝까지 막힘없이 개발할 수 있는 환경 구축 실습: "Hello Fullstack" 로컬 실행 + Git 저장소 초기화

📋 이번 시간 목차
IDE 세팅: Cursor / VS Code
필수 Extensions 설치
Formatter & Linter 설정
터미널: Warp 설치와 활용
Opencode 개념과 워크플로우
프로젝트 구조와 폴더링
환경변수 (.env) 기본
실습: Hello Fullstack
1️⃣ IDE 세팅: Cursor / VS Code
VS Code란?
Microsoft가 만든 무료 코드 에디터
가장 많은 개발자가 사용
확장 프로그램(Extensions)으로 기능 확장
Cursor란?
VS Code를 기반으로 만든 AI 코딩 에디터
AI가 코드 작성을 도와줌
바이브코딩에 최적화
설치
VS Code
# macOS (Homebrew)
brew install --cask visual-studio-code

# 또는 공식 사이트에서 다운로드
# https://code.visualstudio.com/

복사
Cursor
# macOS (Homebrew)
brew install --cask cursor

# 또는 공식 사이트
# https://cursor.sh/

복사
기본 설정 (settings.json)

Cmd + Shift + P
클릭하여 복사
 → Preferences: Open User Settings (JSON)
클릭하여 복사

{
  // 에디터 기본
  "editor.fontSize": 14,
  "editor.fontFamily": "'JetBrains Mono', 'Fira Code', Menlo, monospace",
  "editor.fontLigatures": true,
  "editor.tabSize": 2,
  "editor.insertSpaces": true,
  "editor.wordWrap": "on",
  "editor.lineHeight": 1.6,

  // 저장 시 자동 포맷
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",

  // 파일
  "files.autoSave": "onFocusChange",
  "files.trimTrailingWhitespace": true,
  "files.insertFinalNewline": true,

  // 터미널
  "terminal.integrated.fontSize": 13,

  // 테마 (선택)
  "workbench.colorTheme": "One Dark Pro",
  "workbench.iconTheme": "material-icon-theme"
}

복사
2️⃣ 필수 Extensions 설치
🔴 필수 (반드시 설치)
Extension	용도
Prettier - Code formatter	코드 자동 정리
ESLint	JavaScript/TypeScript 문법 검사
Auto Rename Tag	HTML 태그 자동 수정
Path Intellisense	파일 경로 자동완성
GitLens	Git 히스토리 시각화
🟡 권장 (생산성 향상)
Extension	용도
Material Icon Theme	파일 아이콘 예쁘게
One Dark Pro	인기 테마
Error Lens	에러를 코드 옆에 표시
Auto Import	import 자동 추가
Better Comments	주석 색상 구분
Todo Highlight	TODO 강조
Bracket Pair Color	괄호 색상 구분
🟢 프레임워크별
Extension	용도
ES7+ React/Redux/React-Native snippets	React 코드 조각
Tailwind CSS IntelliSense	Tailwind 자동완성
Prisma	Prisma 문법 지원
Thunder Client	API 테스트 (Postman 대체)
설치 명령어 (터미널)
# 필수 Extensions 일괄 설치
code --install-extension esbenp.prettier-vscode
code --install-extension dbaeumer.vscode-eslint
code --install-extension formulahendry.auto-rename-tag
code --install-extension christian-kohler.path-intellisense
code --install-extension eamodio.gitlens
code --install-extension pkief.material-icon-theme

복사
3️⃣ Formatter & Linter 설정
Prettier 설정

프로젝트 루트에 .prettierrc
클릭하여 복사
 파일 생성:

{
  "semi": false,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100,
  "bracketSpacing": true,
  "arrowParens": "avoid",
  "endOfLine": "lf"
}

복사
Prettier 무시 파일 (.prettierignore
클릭하여 복사
)
node_modules
.next
dist
build
*.min.js
package-lock.json

클릭하여 복사
복사
ESLint 설정

프로젝트 루트에 .eslintrc.json
클릭하여 복사
 파일 생성:

{
  "env": {
    "browser": true,
    "es2021": true,
    "node": true
  },
  "extends": [
    "eslint:recommended",
    "next/core-web-vitals",
    "prettier"
  ],
  "parserOptions": {
    "ecmaVersion": "latest",
    "sourceType": "module"
  },
  "rules": {
    "no-unused-vars": "warn",
    "no-console": "warn",
    "prefer-const": "error"
  }
}

복사
TypeScript용 ESLint
{
  "extends": [
    "eslint:recommended",
    "plugin:@typescript-eslint/recommended",
    "next/core-web-vitals",
    "prettier"
  ],
  "parser": "@typescript-eslint/parser",
  "plugins": ["@typescript-eslint"],
  "rules": {
    "@typescript-eslint/no-unused-vars": "warn",
    "@typescript-eslint/no-explicit-any": "warn"
  }
}

복사
설정 확인
# Prettier 실행 테스트
npx prettier --check .

# ESLint 실행 테스트
npx eslint .

복사
4️⃣ 터미널: Warp 설치와 활용
Warp란?
차세대 터미널 앱
AI 명령어 제안 기능
명령어 자동완성, 히스토리 검색
블록 기반 출력 (복사하기 쉬움)
설치
# macOS (Homebrew)
brew install --cask warp

# 또는 공식 사이트
# https://www.warp.dev/

복사
핵심 기능
기능	단축키	설명
AI 명령어	Ctrl + ``
클릭하여 복사
	자연어로 명령어 생성
블록 선택	Cmd + Up/Down
클릭하여 복사
	출력 블록 이동
히스토리 검색	Ctrl + R
클릭하여 복사
	이전 명령어 검색
자동완성	Tab
클릭하여 복사
	파일/명령어 자동완성
기본 터미널 명령어 복습
# 현재 디렉토리
pwd

# 디렉토리 이동
cd 폴더명
cd ..          # 상위 폴더
cd ~           # 홈 디렉토리

# 목록 보기
ls             # 기본
ls -la         # 상세 + 숨김파일

# 파일/폴더 생성
mkdir 폴더명
touch 파일명

# 복사/이동/삭제
cp 원본 대상
mv 원본 대상
rm 파일명
rm -rf 폴더명   # 폴더 삭제 (주의!)

# 파일 내용 보기
cat 파일명
less 파일명

# 검색
grep "텍스트" 파일명
find . -name "*.js"

복사
5️⃣ Opencode 개념과 워크플로우
Opencode란?
AI 기반 코드 생성 도구들의 통칭
자연어로 코드 작성, 리팩토링, 디버깅
대표적인 AI 코딩 도구
도구	특징
Cursor	VS Code 기반, 가장 인기
GitHub Copilot	GitHub + OpenAI, 자동완성 특화
Claude Code	Anthropic, CLI 기반
ChatGPT	범용, 코드 설명/생성
v0	Vercel, UI 컴포넌트 생성
바이브코딩 워크플로우
1. 요구사항 정리
   ↓
2. AI에게 프롬프트 작성
   ↓
3. 생성된 코드 검토
   ↓
4. 테스트 & 수정
   ↓
5. 커밋

클릭하여 복사
복사
효과적인 프롬프트 작성법
❌ 나쁜 예시:
"로그인 만들어줘"

✅ 좋은 예시:
"Next.js 14 App Router 기준으로
카카오 OAuth 로그인 기능을 만들어줘.
- NextAuth.js 사용
- 로그인 후 /dashboard로 리다이렉트
- 에러 처리 포함
- TypeScript 사용"

클릭하여 복사
복사
프롬프트 핵심 요소
기술 스택 명시: Next.js 14, TypeScript, Tailwind 등
범위 지정: 어떤 파일/기능에 한정할지
예시 제공: 입력/출력 예시
제약 조건: 라이브러리 제한, 스타일 가이드
6️⃣ 프로젝트 구조와 폴더링
Next.js 14 App Router 기본 구조
my-project/
├── app/                    # 페이지 & 라우팅
│   ├── layout.tsx          # 전체 레이아웃
│   ├── page.tsx            # 메인 페이지 (/)
│   ├── globals.css         # 전역 스타일
│   ├── api/                # API 라우트
│   │   └── hello/
│   │       └── route.ts
│   ├── dashboard/          # /dashboard 페이지
│   │   ├── layout.tsx
│   │   └── page.tsx
│   └── (auth)/             # 그룹 라우팅
│       ├── login/
│       └── register/
│
├── components/             # 재사용 컴포넌트
│   ├── ui/                 # 기본 UI (버튼, 인풋 등)
│   ├── layout/             # 레이아웃 컴포넌트
│   └── features/           # 기능별 컴포넌트
│
├── lib/                    # 유틸리티 & 설정
│   ├── utils.ts            # 헬퍼 함수
│   ├── db.ts               # DB 연결
│   └── auth.ts             # 인증 설정
│
├── hooks/                  # 커스텀 훅
│   └── useAuth.ts
│
├── types/                  # TypeScript 타입
│   └── index.ts
│
├── public/                 # 정적 파일 (이미지 등)
├── prisma/                 # DB 스키마 (Prisma 사용 시)
│
├── .env.local              # 환경변수 (개발)
├── .env.production.local   # 환경변수 (운영)
├── .gitignore
├── package.json
├── tsconfig.json
└── next.config.js

클릭하여 복사
복사
폴더링 규칙
폴더	역할
app/
클릭하여 복사
	라우팅 & 페이지 (Next.js 규칙)
components/
클릭하여 복사
	재사용 UI 컴포넌트
lib/
클릭하여 복사
	유틸리티, 설정, 외부 연동
hooks/
클릭하여 복사
	React 커스텀 훅
types/
클릭하여 복사
	TypeScript 타입 정의
public/
클릭하여 복사
	정적 파일 (이미지, 폰트)
파일 네이밍 규칙
컴포넌트:    PascalCase.tsx   (Button.tsx, UserCard.tsx)
훅:         camelCase.ts     (useAuth.ts, useFetch.ts)
유틸:       camelCase.ts     (formatDate.ts, utils.ts)
상수:       UPPER_CASE.ts    (constants.ts 내부)

클릭하여 복사
복사
7️⃣ 환경변수 (.env) 기본
환경변수란?
코드에 직접 적으면 안 되는 민감한 정보
API 키, DB 비밀번호, 시크릿 키 등
환경(개발/운영)에 따라 다른 값 설정
Next.js 환경변수 파일
파일	환경	Git
.env
클릭하여 복사
	모든 환경 기본값	❌ (gitignore)
.env.local
클릭하여 복사
	로컬 개발	❌
.env.development
클릭하여 복사
	개발 환경	⚠️
.env.production
클릭하여 복사
	운영 환경	⚠️
.env.example
클릭하여 복사
	템플릿	✅
환경변수 작성법
# .env.local

# 데이터베이스
DATABASE_URL="postgresql://user:pass@localhost:5432/mydb"

# 인증
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here"

# 외부 API
KAKAO_CLIENT_ID="your-kakao-client-id"
KAKAO_CLIENT_SECRET="your-kakao-secret"

# 브라우저에서 접근 가능 (NEXT_PUBLIC_ 접두사)
NEXT_PUBLIC_API_URL="http://localhost:3000/api"
NEXT_PUBLIC_SITE_NAME="My App"

복사
사용법
// 서버에서만 접근
const dbUrl = process.env.DATABASE_URL

// 클라이언트에서도 접근 (NEXT_PUBLIC_ 필수)
const apiUrl = process.env.NEXT_PUBLIC_API_URL

복사
.gitignore 필수 추가
# 환경변수 (절대 커밋 금지)
.env
.env.local
.env.*.local

복사
.env.example 템플릿
# .env.example (이건 커밋 OK)

DATABASE_URL="your-database-url"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

복사
8️⃣ 실습: Hello Fullstack
목표
Next.js 프로젝트 생성
프론트엔드 페이지 + 백엔드 API 만들기
Git 저장소 초기화 & 첫 커밋
Step 1: 프로젝트 생성
# Next.js 프로젝트 생성
npx create-next-app@latest hello-fullstack

# 설정 선택
✔ Would you like to use TypeScript? Yes
✔ Would you like to use ESLint? Yes
✔ Would you like to use Tailwind CSS? Yes
✔ Would you like to use `src/` directory? No
✔ Would you like to use App Router? Yes
✔ Would you like to customize the default import alias? No

# 프로젝트 폴더로 이동
cd hello-fullstack

복사
Step 2: 개발 서버 실행
npm run dev

복사

브라우저에서 http://localhost:3000
클릭하여 복사
 확인

Step 3: 메인 페이지 수정

app/page.tsx
클릭하여 복사
:

export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-500 to-purple-600">
      <div className="text-center text-white">
        <h1 className="text-5xl font-bold mb-4">
          Hello, Fullstack! 🚀
        </h1>
        <p className="text-xl opacity-80">
          프론트엔드 + 백엔드 첫 번째 프로젝트
        </p>
      </div>
    </main>
  )
}

복사
Step 4: API 라우트 만들기

app/api/hello/route.ts
클릭하여 복사
:

import { NextResponse } from 'next/server'

export async function GET() {
  return NextResponse.json({
    message: 'Hello from API!',
    timestamp: new Date().toISOString(),
    version: '1.0.0'
  })
}

복사
Step 5: API 호출하는 페이지

app/api-test/page.tsx
클릭하여 복사
:

'use client'

import { useState } from 'react'

export default function ApiTestPage() {
  const [data, setData] = useState<any>(null)
  const [loading, setLoading] = useState(false)

  const fetchApi = async () => {
    setLoading(true)
    try {
      const res = await fetch('/api/hello')
      const json = await res.json()
      setData(json)
    } catch (error) {
      console.error('API 호출 실패:', error)
    } finally {
      setLoading(false)
    }
  }

  return (
    <main className="min-h-screen p-8 bg-gray-100">
      <div className="max-w-md mx-auto bg-white rounded-xl shadow-lg p-6">
        <h1 className="text-2xl font-bold mb-4">API 테스트</h1>

        <button
          onClick={fetchApi}
          disabled={loading}
          className="w-full bg-blue-500 hover:bg-blue-600 text-white font-bold py-3 px-4 rounded-lg disabled:opacity-50"
        >
          {loading ? '로딩 중...' : 'API 호출하기'}
        </button>

        {data && (
          <div className="mt-4 p-4 bg-gray-50 rounded-lg">
            <pre className="text-sm overflow-auto">
              {JSON.stringify(data, null, 2)}
            </pre>
          </div>
        )}
      </div>
    </main>
  )
}

복사
Step 6: Git 초기화 & 첫 커밋
# Git 저장소 초기화 (이미 되어 있을 수 있음)
git init

# 상태 확인
git status

# 모든 파일 스테이징
git add .

# 첫 커밋
git commit -m "feat: Hello Fullstack 프로젝트 초기 설정

- Next.js 14 + TypeScript + Tailwind CSS
- 메인 페이지 UI 구현
- /api/hello API 라우트 생성
- API 테스트 페이지 추가"

# 커밋 확인
git log --oneline

복사
Step 7: GitHub 연결 (선택)
# GitHub에서 레포지토리 생성 후
git remote add origin https://github.com/username/hello-fullstack.git
git branch -M main
git push -u origin main

복사
✅ 오늘 배운 것 정리
항목	내용
IDE	Cursor / VS Code 설치 & 설정
Extensions	Prettier, ESLint, GitLens 등 필수 확장
Formatter	Prettier 설정 (.prettierrc)
Linter	ESLint 설정 (.eslintrc.json)
터미널	Warp 설치 & 기본 명령어
폴더 구조	Next.js App Router 구조 이해
환경변수	.env 파일 작성법 & 보안
실습	Hello Fullstack 프로젝트 완성
📚 과제

개인 개발 환경 완성하기

VS Code/Cursor에 모든 필수 Extensions 설치
Prettier, ESLint 설정 파일 프로젝트에 추가

터미널 익숙해지기

Warp 설치 후 기본 명령어 10개 연습
프로젝트 폴더 탐색 연습

Hello Fullstack 확장

새로운 API 라우트 /api/user
클릭하여 복사
 만들기
페이지에서 해당 API 호출하기
🔗 참고 자료
VS Code 공식 문서
Cursor 공식 문서
Next.js 공식 문서
Prettier 설정 가이드
ESLint 설정 가이드

다음 시간: 프론트/백엔드 개념 + Node.js 기초