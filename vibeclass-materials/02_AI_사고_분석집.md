AI 코딩 사고 실제 로그 기반 분석집

대상: Claude Code / Cursor / Copilot을 실제 운영 환경에서 사용하는 개발자·운영자
목적: 실제 장애 로그를 기준으로 원인 → AI 프롬프트 문제 → 재현 → 방지 규칙까지 정리

사고 분석 공통 프레임

모든 사고는 아래 5단계로 분석한다.

장애 로그
실제 증상
AI 프롬프트 문제점
재현 프롬프트
방지 규칙
사례 1️⃣ 로그인 API 간헐적 500 오류
1. 장애 로그
TypeError: Cannot read properties of undefined (reading 'id')
    at AuthService.login (/app/src/auth.service.ts:87)

복사
2. 실제 증상
특정 사용자만 로그인 실패
QA/개발 환경에서는 재현 안 됨
3. AI 프롬프트 문제
로그인 로직을 더 깔끔하게 정리해줘

복사
❌ 범위 지정 없음
❌ null/edge case 언급 없음
4. 재현 프롬프트 (의도적)
아래 조건에서 오류가 발생하도록 테스트 시나리오를 만들어줘
- 유저 데이터 일부가 누락된 경우

복사
5. 방지 규칙
인증/인가 로직은 null 방어 필수
"깔끔하게" 같은 추상 표현 금지
사례 2️⃣ API 응답 변경으로 프론트 전체 장애
1. 장애 로그
TypeError: response.data.items is undefined

복사
2. 실제 증상
배포 직후 프론트 화면 흰 화면
서버는 정상 상태
3. AI 프롬프트 문제
응답 구조를 더 직관적으로 바꿔줘

복사
4. 재현 프롬프트
기존 응답 구조를 사용하는 클라이언트에서 오류가 발생하는 테스트를 만들어줘

복사
5. 방지 규칙
API 계약 변경 = 버전업 필수
AI에게 "응답 구조 변경 금지" 명시
사례 3️⃣ 성능 리팩토링 후 트래픽 폭주 시 장애
1. 장애 로그
Error: connect ETIMEDOUT
    at TCPConnectWrap.afterConnect

복사
2. 실제 증상
평소 정상
트래픽 증가 시 DB 연결 고갈
3. AI 프롬프트 문제
이 코드를 더 빠르게 만들어줘

복사
4. 재현 프롬프트
동시 요청 1000건 상황을 가정해서 병목을 재현해줘

복사
5. 방지 규칙
성능 개선 요청 시 부하 조건 명시
커넥션 풀 설정 검토 필수
사례 4️⃣ 로그 제거로 장애 원인 추적 불가
1. 장애 로그
(no logs available)

복사
2. 실제 증상
장애 발생했으나 원인 파악 불가
3. AI 프롬프트 문제
불필요한 로그를 정리해줘

복사
4. 재현 프롬프트
로그가 없어서 원인 분석이 불가능한 상황을 만들어줘

복사
5. 방지 규칙
운영 로그 제거 금지
로그 레벨(INFO/WARN/ERROR) 명시
사례 5️⃣ 보안 로직 단순화로 취약점 발생
1. 장애 로그
Unauthorized access detected

복사
2. 실제 증상
권한 없는 사용자 접근 가능
3. AI 프롬프트 문제
인증 로직을 단순하게 만들어줘

복사
4. 재현 프롬프트
권한 검사가 누락되는 경로를 의도적으로 만들어줘

복사
5. 방지 규칙
보안/인증 로직은 AI 단독 수정 금지
반드시 사람 리뷰 + 테스트
사고 유형별 요약
유형	원인
인증 오류	null/권한 방어 부족
프론트 장애	API 계약 파괴
성능 장애	부하 조건 미지정
운영 장애	로그 제거
보안 사고	단순화 프롬프트
운영자용 사고 방지 체크리스트
 프롬프트에 범위가 있는가?
 추상 표현("깔끔하게", "더 좋게")을 썼는가?
 기존 계약(API/스키마)을 유지하는가?
 로그/보안 코드를 건드리는가?
 이 변경을 테스트로 재현 가능한가?
마지막 결론

AI는 사고를 만들지 않는다. 사고를 부르는 프롬프트가 있을 뿐이다.

로그를 기준으로 분석할 수 있으면, 그 팀은 이미 시니어 팀이다.